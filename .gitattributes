# Normalize line endings to LF in the repo and on checkout, so the
# prettier `endOfLine: "lf"` gate passes on Windows checkouts (where
# core.autocrlf would otherwise produce CRLF). Linux/macOS are LF
# already, so this is a no-op for existing content.
* text=auto eol=lf
